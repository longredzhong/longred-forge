# Prefix.dev Configuration
# Get PREFIX_TOKEN from https://prefix.dev (Account → API Tokens)
# Create token with "Upload" and "Delete" permissions
PREFIX_TOKEN=your_prefix_token_here
PREFIX_DEV_CHANNEL=longred-forge

# Optional: Override API endpoints if using self-hosted prefix.dev
# PREFIX_DEV_REST_ENDPOINT=https://prefix.dev/api/v1
# PREFIX_DEV_GRAPHQL_ENDPOINT=https://prefix.dev/api/graphql

# GitHub Configuration (for CI)
# GITHUB_TOKEN is automatically available in GitHub Actions
